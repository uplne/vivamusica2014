<div class="thumbnaillist">
    <ul class="thumbnaillist__list">
        {{#foreach program}}
        <li class="thumbnaillist__item">
            <a href="/program/{{path}}" class="thumbnaillist__link">
                <img src="{{img}}" width="190" height="190" alt="" />
                <p class="thumbnaillist__date">{{datenum}}. <span>{{datemonth}}</span></p>
                <p class="thumbnaillist__title">{{title}}</p>
            </a>
        </li>
        {{/foreach}}
    </ul>
</div>
